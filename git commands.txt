Right click- > Git Bash here

//To create repository
git init


// to add origin
git remote add origin <link

//to pull file
git pull origin master

//to push files
git push

git status

git add <file_name>

git commit -m "Adding first commit in local repo"

//to add All files at once
git add -A 

git commit -a -m "commiting all files"

got log

//you cannot commit without giving message


git branch firstbranch
git checkout firstbranch

//while merging branch, make sure you are checked out in destination i.e. master branch

ssh-keygen

copy generated key in GitHub setting-> SSH key

ssh -T git@github.com

git checkout firstbranch

git push origin firstbranch

